# Dependencies and venv
.venv/
.venv-test/
venv/
# Leftover when .venv rename/replace fails mid-delete (locks); do not commit
.venv_broken_*/
env/

# Python bytecode (never commit)
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
agent/.pytest_cache/
agent/.ruff_cache/
*.egg-info/
.eggs/

# Local runtime and data (do not commit)
conversation_history.json
layla.db
layla.db-shm
layla.db-wal
.layla_transport_paired.json
learnings.json
*.db
agent.zip
data/
.zed

# Local config (each user has their own model path / settings)
agent/runtime_config.json
agent/runtime_config.pre-audit-backup.json

# Per-user runtime state (machine-specific paths, scanned-file cache) — must not
# be committed; it hardcodes the author's workspace_root and leaks the username.
agent/.layla/

# Discord bot per-guild state (local)
discord_bot/discord_bot_state.json

# Temp image uploads (agent image context)
.layla_temp_images/

# Fabrication assist local sessions (JSON under repo)
fabrication_assist/.assist_sessions/

# Playwright persistent profiles (local browser state)
agent/.browser_profiles/

# Research lab outputs (generated at runtime, can be large)
agent/.research_lab/
agent/.research_brain/
agent/.research_output/
agent/.capability_discovery_cache/
agent/.capability_sandbox/
agent/.governance/

# Personal identity model (curated self-knowledge, not for public repo)
# The base self_model.md is part of the project; user-added identity files stay local
# NOTE: `.identity/*`, NOT `.identity/`. Git cannot re-include a file whose PARENT DIRECTORY is excluded, so
# with `.identity/` the negations below are silently inert — self_model.md only survived because it was added
# before this rule existed. Ignoring the CONTENTS instead lets the negations actually work.
.identity/*
!.identity/self_model.md
# capabilities.md is project content too, not operator state: it is the VERIFIED manifest of what Layla can
# actually do, and it must ship on clone or "preloaded self-knowledge" silently becomes "no self-knowledge"
# and she goes back to inventing capabilities. User-added identity files still stay local.
!.identity/capabilities.md

# Model files (large; each dev supplies their own)
models/*
!models/.gitkeep

# Chroma / vector store persistence (if at repo root)
chroma/
agent/layla/memory/chroma_db/
# Stray Chroma dir if vector_store ever pointed at agent/chroma_db (do not commit index blobs)
agent/chroma_db/
*.sqlite3

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Knowledge: personal content not committed by default; curated project docs are exceptions below
# Curated starter pack (onboarding only): knowledge/starter/*.md — not matched by knowledge/*.md; keep personal files out of starter/
knowledge/*.txt
knowledge/*.md
knowledge/fetched/
knowledge/docs/
# Curated base knowledge — committed as part of the project
!knowledge/README.md
!knowledge/layla-vision.md
!knowledge/python-best-practices.md
!knowledge/local-ai-models-guide.md
!knowledge/rag-and-memory.md
!knowledge/tools-reference.md
!knowledge/troubleshooting.md
!knowledge/aspects-and-memory.md
!knowledge/morrigan-engineering.md
!knowledge/morrigan-python-stdlib.md
!knowledge/morrigan-fabrication-geometry.md
!knowledge/nyx-research.md
!knowledge/nyx-research-databases.md
!knowledge/echo-behavioral-patterns.md
!knowledge/echo-psychology-frameworks.md
!knowledge/eris-creative-thinking.md
!knowledge/eris-music-and-narrative.md
!knowledge/lilith-ethics-autonomy.md
!knowledge/lilith-philosophy-complete.md
!knowledge/cassandra-pattern-perception.md
!knowledge/cassandra-cognitive-biases.md
!knowledge/cassandra-voice.txt
!knowledge/stack.txt
!knowledge/KNOWLEDGE_LIBRARY_FULL.md
!knowledge/layla-capabilities-map.md
!knowledge/layla-expansion-roadmap.md
!knowledge/fabrication-assist-layer.md
!knowledge/devops-cicd-patterns.md
!knowledge/git-workflow-practices.md
!knowledge/testing-strategy.md
!knowledge/security-engineering-basics.md
!knowledge/api-design-patterns.md
!knowledge/observability-operations.md
!knowledge/containers-orchestration.md

# Repo-specific / handoff docs (keep locally if needed)
REPO_ANALYSIS_FOR_HANDOFF.md

# Vendor / nested repos (each dev clones if needed)
llama.cpp/

# Cursor / IDE: `.cursor/rules/` is tracked in this repo for shared agent guidance; only uncomment `.cursor/` to ignore the whole folder locally.
# .cursor/
docs/archive/AI_HANDOFF_*.md

# Claude Code / AI agent worktrees and settings (local)
.claude/

# Reference zip archives (checked-out locally, not needed in repo)
*.zip

# Extracted reference source trees (local research copies)
integrations/claude-code-sourcemap-main/
integrations/claw-code-main/
integrations/openclaude-main/
# Same archives sometimes extracted at repo root (zip default path)
/claude-code-sourcemap-main/
/claw-code-main/
/openclaude-main/

# Coverage (pytest-cov)
.coverage
agent/.coverage
htmlcov/

# Runtime logs (auto-generated)
*.log
logs/

# Empty scaffolding dirs
# ANCHORED to the repo root on purpose. A bare `sandbox/` matches a directory of that name at ANY
# depth, and it was silently claiming `agent/services/sandbox/` — which holds shell_runner.py, the
# single owner of the shell command blocklist, and python_runner.py. Those files predate the rule so
# they stay tracked (tracked files override .gitignore, and `git add -A` still stages edits to them),
# but `git add <path>` on them fails without -f, and a NEW file added to that package would be
# ignored and never committed. A new security module dropped there would simply vanish.
# Same class as the `.identity/` negation this phase already recorded: git cannot re-include a path
# whose parent directory is excluded, so the exclusion has to be written precisely in the first place.
# The leading slash confines this to a root-level scaffolding dir, which is all it was ever for.
/sandbox/

# GPT Project context pack (generated by scripts/build_gpt_context_pack.py)
layla-gpt-context.zip

# PyInstaller / Windows installer build output
/dist/
/build/
installer/payload/
installer/output/

# Runtime-generated data + DB/vector backups (operator/machine state, never committed)
/backups/
agent/services/.layla/
**/.layla/
agent/all_routes.txt

# Operator cluster state (contains cluster_secret_hash; per-machine, not for the repo)
agent/cluster_config.json

# Operator runtime state (per-machine, mutated at runtime — must never be committed)
# Trailing * covers the .bak sibling too: the rule used to name the .graphml exactly, so
# knowledge_graph.graphml.bak (a byte-identical copy of the ignored graph) was left untracked but
# NOT ignored — one `git add -A` from being committed.
agent/layla/memory/knowledge_graph.graphml*
*.graphml.bak
# Operator state: local conversation titles + the live benchmark scorecard written on every run.
agent/titles.txt
benchmarks/scorecard_live.json
agent/layla/memory/vector_meta.json
agent/runtime_config.json.audit-aside
agent/runtime_config.json.audit-*
# ci-gate scratch. .ci-gate-rc.bak is a BYTE-IDENTICAL copy of the ignored runtime_config.json
# above — secret-shaped fields (discord/slack/telegram tokens, tailscale key, remote_api_key)
# included. The gate uses a DETERMINISTIC path because a random mktemp name cannot be found again
# after a crash (that is what made orphan-repair possible), but audit-loop.mjs tells its agent
# ".planning/audit/* is safe to commit" and auto-pushes when green — so without this rule the
# operator's config is one `git add` from the repo. Exactly the .graphml.bak lesson above.
.planning/audit/.ci-gate*
