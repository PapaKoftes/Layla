#!/usr/bin/env bash
# Stub: full .app packaging requires python-build-standalone + codesign workflow.
set -euo pipefail
echo "macOS bundle build is not automated in this repo yet."
echo "Use the developer install: ./install.sh && ./start.sh"
exit 0
