#!/usr/bin/env bash
# Stub: AppImage build requires linuxdeploy and embedded Python.
set -euo pipefail
echo "Linux AppImage build is not automated in this repo yet."
echo "Use the developer install: ./install.sh && ./start.sh"
exit 0
