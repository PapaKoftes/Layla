# gitleaks false positives — reviewed, confirmed non-secrets.
# Format: <commit>:<file>:<rule>:<line>  (the fingerprint gitleaks prints/reports)
#
# A fake JWT test vector for the jwt_decode tool's smoke test. It decodes to the trivial payload
# {"sub":"1"} (header {"alg":"HS256"}, signature literally "x") — a test fixture, never a credential.
# gitleaks' generic-api-key rule matched the `token=` kwarg. The live line also carries an inline
# `# gitleaks:allow`; this entry covers the historical commit that introduced it (history scan).
#
# NOTE: the fingerprint's commit SHA must track history rewrites. The original 497d0fb6… entry went
# stale after `git filter-repo` re-hashed every commit (the introducing commit is now 72d6cff2…),
# which silently reopened this finding and turned the whole Secret-scan CI job red. Re-pin after any
# future history rewrite by running gitleaks and copying the printed Fingerprint.
72d6cff2b790f3803afedb19af7a019ce11214e0:agent/tests/test_registry_smoke.py:generic-api-key:400
