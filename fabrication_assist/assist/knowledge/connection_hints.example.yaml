# Example connection-style hints for variant stubs (heuristics only, not authoritative CAM).
connection_hints:
  tab_slot: { complexity: low, disassembly: partial }
  screw_boss: { complexity: medium, disassembly: full }
  dovetail: { complexity: high, disassembly: difficult }
