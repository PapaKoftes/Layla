# Layla correctness audit — 2025-03-14

Structured pass after OpenClaw-alignment work: transports hardening, doctor gateway check, bridge docs.

## Automated

| Check | Result |
|--------|--------|
| `cd agent && python -m pytest tests/ -q` | **139 passed**, 1 skipped (environment-specific) |

New tests: `agent/tests/test_transport_inbound.py` (allowlist, pairing, misconfigured deny).

## Invariants (manual / grep)

| Invariant | Status |
|-----------|--------|
| Dangerous tools use `require_approval` / approval flow in `agent_loop` | **OK** — registry encodes `require_approval`; `approval_required` path unchanged |
| Config hot path uses `runtime_safety.load_config()` | **OK** for agent loop / transports inbound (reads config + env) |
| Direct `runtime_config.json` reads | **Pre-existing** in `system_doctor.py` (model dir), `download_docs.py`, installers, `main.py` merge endpoint, etc. — acceptable for bootstrap/doctor; **not** introduced by this change |

## Security checklist (manual)

| Item | Notes |
|------|--------|
| CORS / remote | Unchanged; see `docs/REMOTE_ARCHITECTURE.md` when exposing beyond localhost |
| Transport tokens | Remain env or gitignored `runtime_config.json` — do not commit |
| Inbound DMs | **Mitigated** when `LAYLA_TRANSPORT_ALLOWLIST` and/or `LAYLA_TRANSPORT_PAIRING_SECRET` set, or `transport_require_allowlist` forces explicit policy |
| `/agent` payload size | Unchanged — consider reverse-proxy limits if exposed |
| Pairing file | `.layla_transport_paired.json` gitignored (user ids only) |

## Deliverables (this audit)

- `docs/OPENCLAW_ALIGNMENT.md` — feature matrix
- `docs/OPENCLAW_BRIDGE.md` — HTTP sidecar to `POST /agent`
- `transports/base.py` — `check_transport_inbound`, paired store, RLock fix for nested save
- `agent/services/system_doctor.py` — optional `openclaw_gateway_url` → `GET …/health`
- `agent/runtime_safety.py` + `runtime_config.example.json` — `transport_*`, `openclaw_gateway_url`
- `docs/RUNBOOKS.md` — OpenClaw cross-links
- `ARCHITECTURE.md`, `docs/IMPLEMENTATION_STATUS.md` — updated

## Deferred / follow-ups

- **Lint**: `ruff` / `mypy` not run in this pass; add to CI if desired.
- **system_doctor** model path still reads JSON file directly for report section — could switch to `load_config()` for consistency (cosmetic).
- **OpenClaw `/health` path** — doctor assumes `{base}/health` when URL has no path; set full URL in config if your gateway differs.

## Severity summary

| Severity | Count | Topic |
|----------|-------|--------|
| Fixed in-session | 1 | Transport pairing deadlock (`Lock` → `RLock`) |
| Low | 0 | — |
