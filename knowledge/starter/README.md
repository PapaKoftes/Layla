# Starter knowledge pack

Curated, **non-personal** onboarding text shipped with the repo under `knowledge/starter/`. Index it like any other knowledge (see main Layla docs).

This folder is the **starter** pack only; other paths under `knowledge/` remain local-only unless individually excepted in `.gitignore`.
