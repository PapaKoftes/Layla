---
priority: support
domain: coding
aspects: morrigan
---

# Git essentials

- Prefer small commits; `git status` / `git diff` before proposing changes.
- Worktrees: optional isolation via `git worktree add` (see `services/worktree_manager.py`).
