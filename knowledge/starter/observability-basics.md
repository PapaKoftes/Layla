---
priority: support
domain: coding
aspects: nyx
---

# Observability basics

- Structured events: `services/observability.py` (`log_tool_call`, `log_execution_trace`).
- Token counters: `GET /usage` aggregates session totals from `llm_gateway`.
