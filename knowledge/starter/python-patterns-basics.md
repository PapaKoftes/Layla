---
priority: support
domain: coding
aspects: morrigan
---

# Python patterns (starter)

- Prefer `pathlib.Path` over string paths; use `.expanduser().resolve()`.
- Type hints on public functions; catch specific exceptions.
- Use `logging.getLogger("layla")` instead of `print` in services.
