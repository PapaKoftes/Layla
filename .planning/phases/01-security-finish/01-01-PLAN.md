---
phase: 01-security-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/services/auth.py
  - agent/runtime_safety.py
  - agent/config_schema.py
  - agent/tests/test_real_client_ip.py
autonomous: true
requirements: [REQ-10]
must_haves:
  truths:
    - "A remote client that prepends a forged X-Forwarded-For entry cannot become the derived client IP used for allowlist/rate-limit/audit."
    - "With tunnel_trusted_proxies empty (default), XFF is not trusted at all — the socket peer is used (current safe behavior preserved)."
    - "Every derived IP is parsed/normalized via ipaddress before it reaches allowlist/rate-limit/audit; unparseable values are rejected."
    - "Cf-Connecting-Ip / True-Client-Ip provider headers remain preferred over XFF when present."
    - "remote_enabled=false path is untouched: real_client_ip is never consulted by the middlewares in local-only mode."
  artifacts:
    - agent/services/auth.py
    - agent/runtime_safety.py
    - agent/config_schema.py
    - agent/tests/test_real_client_ip.py
  key_links:
    - "real_client_ip reads tunnel_trusted_proxies from cfg (threaded in by the two main.py middlewares + ws.py, which already pass headers/socket_host)."
    - "The rightmost-trusted-hop walk feeds the same (client_ip, via_proxy) tuple the allowlist/rate-limit/audit already consume — no caller signature change."
---

<objective>
Replace the spoofable leftmost-XFF derivation in `real_client_ip` (per D — REQ-10) with the canonical
**rightmost-trusted-hop** walk over a new `tunnel_trusted_proxies` config, and normalize every derived IP
via `ipaddress` before it can poison the allowlist / rate-limiter / audit log.

Purpose: closes the residual forwarded-header poisoning gap for chained, header-appending proxies
(cloudflared → nginx → Layla) — the same trust-boundary class as the already-remediated REQ-01, one layer in.
Output: a hardened `real_client_ip`, a new `tunnel_trusted_proxies` default + schema entry, and a pure-stdlib
test file proving a forged hop does not move the derived IP.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-finish/CONTEXT.md
@.planning/research/security-patterns.md

# The unit under change and its only caller-visible contract:
@agent/services/auth.py
# Where tunnel_trusted_proxies default lives (Remote-access block, ~line 488-498) and how load_config merges file over defaults (~line 656-669):
@agent/runtime_safety.py
# The three call sites that already pass (headers, socket_host) and consume (client_ip, via_proxy) — DO NOT change their signatures:
@agent/main.py
@agent/routers/ws.py
# Existing CIDR/ipaddress matching pattern to mirror (is_ip_allowed):
@agent/services/tunnel_auth.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rightmost-trusted-hop derivation in real_client_ip</name>
  <files>agent/services/auth.py, agent/tests/test_real_client_ip.py</files>
  <behavior>
    Add a pure helper `_derive_forwarded_ip(headers, trusted_proxies)` (or inline equivalent) and rewrite the
    XFF branch of `real_client_ip(headers, socket_host, cfg=None)`:
    - Cf-Connecting-Ip / True-Client-Ip / X-Real-Ip preference is unchanged (provider-set, single-value, not client-forgeable) — keep them checked before XFF.
    - For X-Forwarded-For: merge ALL XFF header instances (and the existing `forwarded` parse) into one ordered hop list left→right, then walk RIGHT→LEFT, skipping any hop whose normalized IP is in `tunnel_trusted_proxies` (IPs or CIDRs, matched via ipaddress like is_ip_allowed). The first non-trusted hop is the real client. Mirrors uvicorn ProxyHeadersMiddleware reverse walk.
    - `tunnel_trusted_proxies` defaults to `[]`. When empty: do NOT trust XFF at all — fall through to the socket peer (preserves today's safe behavior). When the request did not arrive on loopback, headers are still ignored entirely (keep the existing `_is_localhost(socket_host)` guard).
    - Validate/normalize EVERY derived IP through `ipaddress.ip_address(...)` (strip `:port` and `[]` brackets / IPv4-mapped forms first). Reject (return socket peer, via_proxy as today's fall-through) any unparseable hop instead of returning a raw string.
    - Signature: add an optional `cfg` param defaulting to None; when None, `tunnel_trusted_proxies` resolves to `[]` (so existing callers that don't yet pass cfg keep the safe default). `via_proxy` semantics unchanged.
    Test cases (test_real_client_ip.py, pure stdlib — runs on 3.14):
    - Test A (regression guard): cfg with empty tunnel_trusted_proxies, XFF `"1.2.3.4, 5.6.7.8"`, loopback socket → derived IP is the SOCKET PEER (XFF untrusted), via_proxy reflects no-trust fall-through.
    - Test B (the attack): cfg trusts the nginx hop `["10.0.0.2"]`, headers `X-Forwarded-For: 6.6.6.6, 9.9.9.9, 10.0.0.2` (attacker prepended `6.6.6.6`), loopback socket → derived IP is `9.9.9.9` (rightmost non-trusted), NOT `6.6.6.6`.
    - Test C (provider header wins): Cf-Connecting-Ip `203.0.113.7` present alongside a forged XFF → derived IP is `203.0.113.7`.
    - Test D (normalization/reject): an XFF hop `"not-an-ip"` in the non-trusted position → not returned as a client IP; falls back safely (assert the result parses as an IP or is the socket peer).
    - Test E (CIDR trust): tunnel_trusted_proxies `["10.0.0.0/8"]`, XFF `8.8.8.8, 10.1.2.3` → derived `8.8.8.8`.
    - Test F (all-trusted fallback safety): every hop is trusted → do NOT silently return the leftmost spoofable entry; return the socket peer (document the uvicorn #1068 pitfall this avoids).
  </behavior>
  <action>
    Rewrite the X-Forwarded-For branch of `real_client_ip` in agent/services/auth.py to the rightmost-trusted-hop
    walk described in the behavior block (per REQ-10 / CONTEXT D). Reuse the CIDR/ipaddress matching shape already in
    services/tunnel_auth.is_ip_allowed (parse with ipaddress.ip_address / ip_network(strict=False); on ValueError,
    skip the entry and log at warning). Add an optional `cfg=None` parameter; read `tunnel_trusted_proxies` from cfg
    (default `[]`). Keep the `_is_localhost(socket_host)` loopback guard and the Cf-Connecting-Ip/True-Client-Ip/
    X-Real-Ip precedence exactly as-is — only the XFF fall-through changes. Add a one-line module note that empty
    tunnel_trusted_proxies means "XFF untrusted, use socket peer" so the safe default is self-documenting.
    Write agent/tests/test_real_client_ip.py with cases A-F as flat pure-stdlib test functions using a tiny
    fake-headers mapping (a dict-like with `.get` and multi-instance XFF support) — no FastAPI, no TestClient,
    no network. Do NOT import runtime_safety in the test; pass cfg dicts directly.
    Do NOT change the (headers, socket_host) call sites in main.py / ws.py in this task — Task 2 threads cfg in.
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_real_client_ip.py -q</automated>
  </verify>
  <done>Cases A-F pass; the forged-prepended-XFF case (B) derives the rightmost non-trusted hop, not the attacker entry; empty-allowlist (A) uses the socket peer; every returned client IP either parses via ipaddress or is the socket peer.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tunnel_trusted_proxies (default + schema) and thread cfg into the three call sites</name>
  <files>agent/runtime_safety.py, agent/config_schema.py, agent/main.py, agent/routers/ws.py</files>
  <action>
    Add `"tunnel_trusted_proxies": []` to the runtime_safety.py defaults dict inside the "Tunnel auth &amp; audit"
    block (next to tunnel_ip_allowlist, ~line 583), so load_config() surfaces it with a safe empty default
    (per REQ-10 / CONTEXT D). Add a matching EDITABLE_SCHEMA entry in config_schema.py under the remote category:
    key `tunnel_trusted_proxies`, type list/string, default empty, hint explaining "IPs/CIDRs of YOUR reverse
    proxies; leave empty to never trust X-Forwarded-For (uses the socket peer). Do not add the real client's
    network here." Then update the three call sites to pass the already-loaded cfg into real_client_ip:
    main.py remote_auth_middleware (~line 762) and remote_rate_limit_middleware (~line 840) already hold `cfg`
    from load_config() — change `real_client_ip(request.headers, _socket_host)` to
    `real_client_ip(request.headers, _socket_host, cfg)`. ws.py `_ws_check_auth` (~line 42) already holds `cfg` —
    change `real_client_ip(websocket.headers, _socket_host)` to `real_client_ip(websocket.headers, _socket_host, cfg)`.
    Leave is_direct_local's internal call passing cfg=None (it is used by the settings.py protected-keys check where
    the safe default is correct). REGRESSION GUARD: do not touch the `if not cfg.get("remote_enabled"): return ...`
    early-return at the top of each middleware — local-only (remote_enabled=false) requests still never reach
    real_client_ip, so their behavior is unchanged.
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_real_client_ip.py tests/test_tunnel_auth.py -q &amp;&amp; python -c "import ast,sys; [ast.parse(open(f,encoding='utf-8').read()) for f in ('main.py','routers/ws.py','config_schema.py','runtime_safety.py','services/auth.py')]; print('parse-ok')"